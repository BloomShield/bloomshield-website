import Link from "next/link";
import { ArrowRight } from "lucide-react";
export function CTA({ title = "Let’s make prevention more equitable.", text = "Work with BloomShield to design practical, evidence-informed approaches that reach the communities who need them most." }:{title?:string;text?:string}) { return <section className="section-space"><div className="container-page"><div className="relative overflow-hidden rounded-4xl bg-teal-800 px-7 py-14 text-white sm:px-12 md:py-16"><div aria-hidden="true" className="absolute -right-16 -top-24 h-72 w-72 rounded-full border-[40px] border-emerald-400/10"/><div className="relative max-w-2xl"><p className="text-sm font-bold uppercase tracking-[.2em] text-emerald-400">Start a conversation</p><h2 className="mt-4 font-display text-3xl font-semibold tracking-tight sm:text-4xl">{title}</h2><p className="mt-5 text-lg leading-8 text-white/75">{text}</p><Link href="/contact" className="mt-8 inline-flex min-h-12 items-center gap-2 rounded-full bg-white px-6 py-3 text-sm font-bold text-teal-900 transition hover:bg-teal-50">Contact BloomShield <ArrowRight size={17}/></Link></div></div></div></section> }
