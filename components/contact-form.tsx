"use client";
import { FormEvent, useState } from "react";
import { LoaderCircle, Send } from "lucide-react";
export function ContactForm(){const [state,setState]=useState<"idle"|"sending"|"sent"|"error">("idle");const [message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");setMessage("");const form=new FormData(e.currentTarget);const data=Object.fromEntries(form.entries());try{const r=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const body=await r.json();if(!r.ok)throw new Error(body.error||"Unable to send");setState("sent");setMessage("Thank you. Your message has been sent to the BloomShield team.");(e.target as HTMLFormElement).reset()}catch(err){setState("error");setMessage(err instanceof Error?err.message:"We could not send your message. Please try the contact form again later.")}}
return <form onSubmit={submit} className="rounded-3xl border border-teal-900/10 bg-white p-6 shadow-soft sm:p-9"><div className="grid gap-5 sm:grid-cols-2"><Field label="Full name" name="name" autoComplete="name"/><Field label="Work email" name="email" type="email" autoComplete="email"/><Field label="Organisation" name="organisation" autoComplete="organization" optional/><label className="block"><span className="text-sm font-bold">How can we help?</span><select name="interest" required defaultValue="" className="mt-2 min-h-12 w-full rounded-xl border border-slate-300 bg-white px-4"><option value="" disabled>Select one</option><option>Programme partnership</option><option>Consultancy or advisory</option><option>Research collaboration</option><option>Speaking or training</option><option>General enquiry</option></select></label></div><label className="mt-5 block"><span className="text-sm font-bold">Your message</span><textarea name="message" required minLength={20} rows={6} className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3" placeholder="Please share a little about your enquiry and what a useful next step would look like."/></label><label className="hidden" aria-hidden="true">Leave this field empty<input name="website" tabIndex={-1} autoComplete="off"/></label><p className="mt-4 text-sm leading-6 text-slate-500">By submitting this form, you agree that BloomShield may use your details to respond to your enquiry. See our <a href="/privacy" className="underline">privacy notice</a>.</p><button disabled={state==="sending"} className="button-primary mt-6 disabled:opacity-60">{state==="sending"?<LoaderCircle className="animate-spin" size={17}/>:<Send size={17}/>}Send enquiry</button>{message&&<p role="status" className={`mt-5 rounded-xl p-4 text-sm ${state==="sent"?"bg-teal-50 text-teal-800":"bg-red-50 text-red-800"}`}>{message}</p>}</form>}
function Field({label,name,type="text",autoComplete,optional=false}:{label:string;name:string;type?:string;autoComplete:string;optional?:boolean}){return <label className="block"><span className="text-sm font-bold">{label}{optional&&<span className="font-normal text-slate-500"> (optional)</span>}</span><input name={name} type={type} required={!optional} autoComplete={autoComplete} className="mt-2 min-h-12 w-full rounded-xl border border-slate-300 px-4"/></label>}
